const Loading = () => {
  return (
    <div className="z-[1000] flex h-screen w-screen items-center justify-center bg-primary">
      <img src="/gifs/loader.gif" alt="Loading..." />
    </div>
  );
};

export default Loading;
